#lambda parameters: expression

double = lambda x: x * 2 #one parameter
print (double(4))

multiply = lambda x, y: x * y  #two parameters
print(multiply(6, 8))

add = lambda x, y, z: x + y + z
print(add(12, 10, 8))